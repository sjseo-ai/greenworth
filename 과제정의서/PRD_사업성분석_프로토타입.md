# PRD — 재무모델 기반 사업성 분석 프로토타입

버전 0.1 (초안) · 작성일 2026-08-31 · 작성자 서신준 (인프라사업2 본부)
기반 기획문서: `과제정의서_3팀_서신준.md`, 서비스 프로세스 다이어그램, `ESS_단가산정모델.xlsx`, `ESS_비가격점수_가격환산_계산기_1.xlsx` / ESS 가격환산 계산기 페이지

---

## 0. 이 문서의 목적

이 PRD는 위 기획문서들에 흩어져 있던 요구사항(핵심 변수 기반 재무모델 자동생성, 발전원별 입력, Sensitivity 분석, ESS 부속 도구 2종)을 하나의 **웹 프로토타입**으로 통합하기 위한 실행 사양이다. 처음부터 완성형을 만들지 않고, **3단계 이터레이션**으로 쪼개 각 단계마다 실제로 켜볼 수 있는 결과물이 나오게 한다. 각 이터레이션은 기획문서 "4. 성공 기준(3단계)"의 최소 성공/목표 성공/이상적 성공에 각각 대응한다.

---

## 1. 배경과 문제

- 현재는 프로젝트 검토마다 엑셀 재무모델을 수기로 만들고, 매크로 변수(금리·환율·물가상승률) 조합별로 반복 재계산해 Sensitivity 표를 손으로 작성한다. 프로젝트당 반나절~1일이 소요되고, 변수 조합이 늘수록 반복 계산 부담이 커진다.
- ESS 사업은 추가로 두 갈래 의사결정이 필요하다: ① KCH가 개발자로서 받을 4개 수수료(부지 임대료·사전개발비·공동접속료·운영수익) 단가를 목표 IRR에 맞춰 정하는 문제, ② ESS 중앙계약시장 입찰에서 입찰가격이 비가격점수 대비 얼마의 가치를 갖는지 환산하는 문제. 현재 이 둘은 각각 별도 엑셀 파일(Goal Seek 수동 실행)로만 존재한다.
- 세 가지 계산(재무모델·단가산정·가격환산)이 서로 다른 파일에 흩어져 있어, 가정을 하나 바꾸면 세 파일을 각각 다시 열어 손으로 맞춰야 한다.

## 2. 목표

| 구분 | 내용 |
| --- | --- |
| 제품 목표 | 핵심 변수만 입력하면 재무모델·투자지표·Sensitivity가 자동 계산되고, ESS의 경우 단가산정·가격환산까지 한 화면 흐름에서 이어지는 웹 프로토타입 |
| 성공 지표 | 프로젝트 1건당 사업성 분석 소요시간을 반나절~1일 → 1~2시간으로 단축 (기획문서 6장과 동일 기준) |
| 비목표 (이번 프로토타입 범위 밖) | 실사용자 인증/권한 관리, 다인 동시편집, 외부 시스템(회계·ERP) 연동, 모바일 전용 UI 최적화 |

## 3. 대상 사용자

- **주 사용자**: 서신준 본인(인프라사업2 본부) — 프로젝트 검토 시 비정기 사용
- **확장 사용자**: 부서 전체 (이터레이션 3에서 "여러 시나리오 저장·비교"가 갖춰진 뒤)

## 4. 범위 밖으로 명시적으로 제외하는 것 (기획문서 "Known model boundaries" 준용)

이 프로토타입은 참고용 사업성 검토 도구이며 감사된 투자자문이 아니다. 아래는 의도적 제외 항목이며, "구현이 빠졌다"는 이유로 임의로 추가하지 않는다.

- P50/P90 발전량 시나리오 선택, 월별/분기별 발전량·가격 곡선
- VAT, 자산별 세부 감가상각, 부채 스컬프팅(sculpting), 리파이낸싱
- 세율 프리셋은 역사적 예시이며 현행 세법을 반영하지 않음(세무자문 아님)
- 태양광: 일사량 시계열, 출력제한(커튼먼트), 인버터 교체 주기
- 육상풍력: 연 단위로 단순화한 검증 프리셋
- 해상풍력: 해저지반·항만·설치선 실측 견적, 월별 발전량 변동
- ESS: 충·방전 시계열, 주파수조정 등 보조서비스 정산, 배터리 효율의 연차별 완만한 하락

---

## 5. 핵심 사용자 시나리오

1. 사용자가 발전원(태양광/육상풍력/해상풍력/ESS)을 고르고 핵심 변수를 입력한다.
2. 시스템이 입력값을 검증(누락·이상값 체크, 업계 표준치 제안)하고 B/S·I/S·CF를 자동 생성한다.
3. IRR·NPV·배당 등 투자지표를 계산하고, 매크로 변수 조합별 Sensitivity를 보여준다.
4. 결과가 기준에 못 미치면 변수를 다시 조정한다(What-if 반복).
5. **(ESS만)** KCH 개발수수료 목표 IRR을 입력하면 4개 수수료 단가가 역산된다.
6. **(ESS만)** 입찰가격을 입력하면 가격평가점수와 비가격점수 1점의 가격 가치가 계산된다.
7. 담당자가 결과를 검토·승인하면 PDF 리포트로 정리되어 부서에 공유된다.

---

## 6. 기능 요구사항

### 6.1 발전원 선택 및 핵심 변수 입력 (공통)

- 발전원 4종(PV/ON/OF/ES) 중 1개 선택. 선택에 따라 입력 필드 세트가 바뀐다.
- 공통 입력 5섹션: 사업개요(설비용량·기간), 발전·매출(단가·이용률 또는 저장조건), CAPEX(7개 범주: 기자재/토목/전기/계통·선로/운송·설치/간접비/토지), OPEX, 금융·세무(WACC·세율·DSCR 등은 **읽기 전용 프리셋**으로 표시).
- 발전원별 차별 필드(다이어그램 기준):
  - PV: P75 이용률, 연간 성능저하율, REC 가중치
  - ON/OF: P75 이용률, REC 가중치, (OF만) 해상 특화 CAPEX 항목
  - ES: 저장시간, 연간 사이클, 왕복효율, 소내소비율, 배터리 증설 주기·단가, LTSA 단가 변경 시점·배율
- 필수 값 누락·범위 이상 시 인라인 경고 + 업계 표준치 제안값 노출.

### 6.2 재무모델 자동 생성 및 투자지표

- 입력값만으로 연도별 B/S·I/S·CF를 구성한다(추정재무제표).
- IRR(세전/세후, 프로젝트/지분), NPV, 배당(가능이익·법정준비금 반영), DSCR 게이트를 계산한다.
- B/S–I/S–CF 간 정합성(자기검증)을 계산 후 표시한다.

### 6.3 Sensitivity 분석

- 매크로 변수(금리·환율·물가상승률 등) 조합별로 재계산해 표/그래프로 보여준다.
- 최소 1개 축(예: 금리) 단일 민감도표는 이터레이션 1부터, 2축 조합(예: 가격×물가상승률) 매트릭스는 이터레이션 2부터.

### 6.4 ESS 단가산정 모델 (KCH 개발수수료)

- 입력: 설비 규모·기간, KCH 비용(용지비·개발비·관리비), 4개 수익원(부지 임대료/사전개발비/공동접속료/운영수익) 기준단가·상승률, 목표 IRR.
- **공통 가격배율** 하나로 4개 단가를 동시에 스케일링하고, 목표 IRR을 만족하는 배율을 자동으로 구한다(수치해석: 이분탐색 또는 뉴턴법 — 엑셀의 Goal Seek을 웹에서는 반복 계산 함수로 대체).
- 가격배율 × 설비용량 조합별 달성 IRR 민감도표(목표 IRR 이상 칸 강조).

### 6.5 ESS 가격환산 계산기

- 산식(전력거래소 공고 제2025-05호 Ⅲ.5 기준): `가격평가점수 = ROUND(최저입찰가격 ÷ 입찰가격 × 가격배점, 2)`
- 비가격점수 1점당 가격 환산: 방법 A(미분근사) `입찰가격² ÷ (최저가격 × 가격배점)`, 방법 B(±1점 정확값의 평균) — 이미 검증된 산식이므로 그대로 이식.
- 점수 변동(Δ점, −5~+5) 민감도표.
- **(이터레이션 2 이후)** 6.4의 목표 IRR 역산 판매단가를 입찰가격의 기본값으로 자동 연결.

### 6.6 결과 출력 및 협업

- 결과 웹페이지: 입력 요약 + 투자지표 + B/S·I/S·CF + Sensitivity 표.
- 변수 재입력 시 즉시 재계산되는 What-if 루프.
- PDF 리포트 자동 생성(투자지표 + 재무제표 + Sensitivity 요약), 필요 시 다운로드/공유.
- 여러 변수 시나리오(프로젝트)를 저장하고 나란히 비교.

---

## 7. 비기능 요구사항

| 항목 | 기준 |
| --- | --- |
| 정확성 | 기존 `js/finance.js` IRR/NPV 계산 로직(이분법 IRR, NPV, 누진세, NOL 이월 등)을 그대로 재사용하거나 동일 알고리즘으로 이식해 결과가 일치해야 함 |
| 민감정보 | 자격증명·개인정보는 입력받지 않음. 실적·계약 금액 등은 마스킹 안내 문구 노출 |
| 성능 | 입력 변경 후 재계산은 200ms 이내(디바운스 포함) — 기존 앱의 120ms 디바운스 관례 준용 |
| 접근성 | 폼 라벨·에러 메시지 스크린리더 대응 (기존 앱의 `aria-describedby` 패턴 준용) |
| 브라우저 | 최신 Chrome/Edge 기준, 별도 서버 없이 `file://` 또는 정적 서버로 구동 가능 |

---

## 8. 아키텍처 원칙 (수정하기 좋은 구조를 위해)

이 프로젝트가 이미 채택 중인 `사업성모델 앱`의 모듈 분리 패턴을 그대로 따른다 — 계산 로직과 화면을 분리해두면 이터레이션마다 한쪽만 건드리고 다른 쪽은 안 건드릴 수 있다.

| 계층 | 파일(안) | 책임 | 비고 |
| --- | --- | --- | --- |
| 데이터 | `project-data.js` | 발전원 4종 기본값, CAPEX 범주 정의 | 기존 파일 재사용 |
| 계산 엔진 | `finance.js` | IRR/NPV/세금/DSCR/배당 계산 | 기존 파일 재사용 |
| **신규 계산** | `ess-pricing.js` | KCH 개발수수료 역산(공통 가격배율 이분탐색) | 이터레이션 2 |
| **신규 계산** | `price-score.js` | 가격평가점수·비가격점수 환산 산식 | 이터레이션 2 (이미 JS로 구현된 계산기 재사용) |
| 폼 화면 | `form-view.js` | 입력 폼 렌더링 | 기존 파일 재사용/확장 |
| 결과 화면 | `results-view.js` | KPI·표·그래프 렌더링 | 기존 파일 확장 |
| **신규 화면** | `sensitivity-view.js` | 2축 민감도표 렌더링 | 이터레이션 2 |
| **신규 화면** | `scenario-store.js` | 시나리오 저장/비교 (localStorage) | 이터레이션 3 |
| **신규 화면** | `report-view.js` | PDF 리포트 생성 | 이터레이션 3 |
| 배선 | `app.js` | 이벤트·상태 관리 | 기존 파일 확장 |

> 원칙: 새 기능은 새 파일로 추가하고 기존 파일은 최소 수정한다. 계산 로직에는 DOM 접근 코드를 넣지 않는다(기존 `finance.js` 관례).

---

## 9. 이터레이션 로드맵

### 이터레이션 1 — 최소 성공: 핵심 흐름 1회 작동

**목표**: "핵심 변수를 입력하면 재무모델과 투자지표가 한 번 자동 계산된다"를 증명한다. (기획문서 성공기준 "최소 성공"에 대응)

**범위**
- 발전원 1종만 지원 (ESS 우선 — 부속 도구 2개와 바로 이어지므로)
- 6.1의 공통 입력 5섹션 폼 (발전원별 차별 필드는 ESS 것만)
- 6.2 재무모델 자동 생성 + IRR/NPV/배당 계산
- 결과를 한 화면에 KPI + 간단한 표로 출력 (그래프·PDF 없음)
- Sensitivity·시나리오 저장·ESS 부속 도구는 이번 단계에 **포함하지 않음**

**완료 조건 (DoD)** — 2026-08-31 검증 완료
- [x] ESS 핵심 변수 입력 폼이 렌더링된다 — 실제 앱을 헤드리스 브라우저로 구동해 ESS 탭 선택 후 확인
- [x] 저장 → 계산 → 결과 표시까지 오류 없이 1회 동작한다 — 분석 결과 패널이 "계산 완료" 상태로 정상 출력
- [x] IRR/NPV 값이 `js/finance.js`의 기존 ESS 프리셋 계산 결과와 오차 없이 일치한다(회귀 테스트로 고정) — `tests/ess-preset-regression.test.mjs` 신규 추가, golden value와 오차 1e-9 이내 일치 확인
- [x] 입력값을 바꾸면 결과가 다시 계산된다(새로고침 없이) — 설비용량 50→80MW 변경 시 P-IRR 3.54%→11.97%, NPV -228.9→282.3억원으로 새로고침 없이 재계산됨을 스크린샷으로 확인

**산출물**: 동작하는 단일 페이지 프로토타입 (발전원 1종, 계산+결과만)

**주요 변인 상세**: 사업개요·발전매출·CAPEX·OPEX·금융세무 5개 섹션의 변수를 "가격환산계산기 ↔ 단가산정모델 ↔ 적정 입찰단가" 체인 관점에서 확정한 초안은 [`변수정의서_초안_이터레이션1.md`](변수정의서_초안_이터레이션1.md) 참조. 목표 IRR → 적정 입찰단가 역산이 이터레이션 1의 새 계산 요구사항으로 추가됨.

**프로토타입 페이지 (2026-08-31)**: 위 변수정의서를 그대로 구현한 단일 페이지 프로토타입을 Artifact로 게시함 — 5개 섹션 입력 폼 + 목표 IRR→적정 입찰단가 역산(이분탐색) + 가격환산계산기 자동 연동. 헤드리스 브라우저로 구동해 콘솔 오류 없음, 목표 IRR 8%/12% 각각 정확히 수렴함을 확인.

**발전매출 산식 확정 (2026-08-31)**: 최초 버전은 판매단가를 "충전→방전 에너지(kWh) × 단가"로 계산했는데, 그 가정으로는 목표 IRR 8% 달성에 필요한 입찰단가가 340~410원/kWh로 나와 가격환산계산기 예시값(18~20원/kWh)과 자릿수가 어긋났다. 2025년 제1차 ESS 중앙계약시장 입찰공고문(전력거래소 공고 제2025-03호)을 확인한 결과, 실제 정산 기준은 "공급가능용량 기준 단위 고정비(원/kW-h)"였다. 사용자 지시에 따라 `연간 매출 = 입찰단가 × 계약용량(MW) × 24h × 365일 × 가동률`로 산식을 교체하니 적정 입찰단가가 약 42~51원/kWh로 나와, 예시값과 자릿수가 맞아떨어졌다(정확한 값은 CAPEX·OPEX 초안이 실제 견적으로 교체되면 달라짐). 공고문의 실제 패널티 산식(일간 미이행률에 따른 정산금 감액)은 사용자 결정에 따라 이번 단계에서는 반영하지 않고, 계산식이 확정되면 추가하기로 함.

**PCS 대수 분리·연차별 가동률·당사 손익 추가 (2026-08-31)**: 사용자 요청에 따라 세 가지를 프로토타입에 반영. ① 1절 "총 PCS 출력용량"이 그동안 배터리 유닛 대수를 그대로 재사용해 계산되던 것을, "PCS 대수" 필드를 신설해 (PCS 대수 × 대당 PCS 출력)으로 배터리 유닛 대수와 독립적으로 계산하도록 분리(총 저장용량은 기존대로 배터리 유닛 대수 기준 유지, CAPEX·OPEX 항목별 배수도 당분간 배터리 유닛 대수 기준 유지 — 상세는 [`변수정의서_초안_이터레이션1.md`](변수정의서_초안_이터레이션1.md) 3절 메모 참조). ② 2절 발전매출의 가동률을 단일값에서 운영연차(1~N년차)별 개별 입력 그리드로 확장하고, `salesForRow`가 `operationSequence`로 해당 연차의 가동률을 조회하도록 계산 로직을 수정("기본 가동률 일괄 적용" 버튼으로 한 번에 채우는 것도 가능). ③ 결과 패널에 "당사(지분) 손익" KPI 4종(당사 누적 출자금·누적 배당+최종회수·누적 순이익 세전/세후·투자배수 MOIC)을 신규 추가 — 지분 현금흐름(`equityFlows`/`equityFlowsAfterTax`)의 단순 합산치로, 시간가치 미반영 보조 지표임을 명시. 헤드리스 브라우저로 PCS 대수 변경 시 저장용량 불변·출력용량만 변동, 가동률 그리드가 운영기간 변경에 따라 재구성되며 편집값이 보존됨, 당사 손익 KPI가 합리적 범위로 표시됨을 각각 확인(콘솔 오류 없음).

**준공지연·이행률 페널티, 공급량 산정 ①~⑤ 단계 구조 반영 (2026-08-31)**: 사용자가 공고문 25p의 "Ⅴ.나. 준공지연 페널티"와 Ⅴ.가의 이행률 패널티(정산 감액)를 매출에 반영해달라고 요청, 아울러 매출 산식을 더 세분화해달라고 요청. 공고문 25p 전체를 읽어 확인한 내용:
- 공급량 산정 구조: ①방전가능용량(MWh)=계약용량×6h → ②일간공급가능량(MWh)=①÷6h×24h → ③가동률[%] → ④일간공급예상량=②×③ → ⑤년간공급예상량=④×365일. 이 ①~⑤ 단계를 프로토타입 2절에 표로 반영(물리적 공급량 산정, 입찰단가와 무관).
- 준공지연 페널티(Ⅴ.나): 지연일수 입력 하나로 1/2/3단계를 자동 판정해 반영. 1단계(≤180일)는 거래기간 15년 유지·계약가격 × (15년×365−지연일수)/(15년×365) 감액(원문 표의 병합 셀이 PDF 텍스트 추출 과정에서 뒤섞여 있어 이 부분은 "초안 해석"으로 명시), 2단계(180일 초과~2년)는 거래기간을 지연일수만큼 단축(계약가격 유지), 3단계(2년 초과)는 계약 해지로 처리해 매출 자체를 발생시키지 않고 결과 패널 전체를 "계약 해지" 배너로 대체.
- 이행률 패널티(Ⅴ.가): 원문은 일간·충전/방전별 급전지시량 대비 계량값 산식이나, 시간대별 지시 데이터가 없어 "연평균 미이행률(%)" 단일값으로 근사 — 물리적 공급량(⑤년간공급예상량)은 그대로 두고 정산금(매출)에만 할인 적용(실제로도 미이행은 "정산금"만 깎지 "공급량"을 깎지 않는다는 점을 UI에 명시).
- 헤드리스 브라우저로 검증: 지연일수 100일(1단계)→적정 입찰단가 41.68→42.46원/kWh 상승(가격조정배율 0.9817 = (5475-100)/5475 정확히 일치), 300일(2단계)→42.71원/kWh(거래기간만 축소, 가격조정 없음), 800일(3단계)→"N/A, 계약 해지" 배너와 가격환산계산기 섹션까지 함께 초기화됨을 확인, 미이행률 20% 입력 시 52.10원/kWh로 상승. 모든 케이스에서 콘솔 오류 없음. 검증 과정에서 "계약 해지/목표IRR 미달" 상태일 때 가격환산계산기 섹션이 직전 값을 그대로 보여주는 기존 버그를 함께 발견해 수정(N/A 상태 전파).
- CAPEX/OPEX·잔존율(경년열화)·운전효율 체인(TR·PCS·Cable 효율)은 이번 범위에 포함하지 않음 — 변수정의서 7절 체크리스트에 반영 필요 여부 확인 항목으로 기록해 사용자가 다음 라운드에서 판단하도록 함.

**설치용량·계약용량 분리 (2026-08-31)**: 사용자가 "지금 나온 용량들은 전부 설치용량 기준이고 계약용량을 기입할 수 있도록 다시 만들어줘, 매출은 계약용량에 연동해서 나오니까 참고해달라"고 지적. 그동안 1절에서 PCS 대수×대당 출력으로 계산한 "총 PCS 출력용량"(장비 스펙 합계, 즉 설치용량)을 2절의 "계약용량"으로 그대로 재사용하고 있었는데, 실제로는 계약(입찰)용량이 설치용량과 다를 수 있다는 문제였다(공고 Ⅰ.가·Ⅳ.가상 계약용량은 10MW 초과~100MW 미만·1MW 단위로 별도 신고). 조치: 1절 필드는 "설치 저장용량"·"설치 출력용량(PCS 정격 합계)"으로 명칭을 명확히 하고, 2절에 "계약용량(입찰물량)"을 독립적인 입력 필드로 신설(설치용량은 참고용으로만 병기, 계약용량이 설치용량을 초과하면 경고). `analyzeEss`·공급량 산정표(①~⑤)의 `capacityMW`를 전부 `model.project.totalPowerMW`(설치용량)에서 `model.revenue.contractCapacityMW`(계약용량)로 교체. 헤드리스 브라우저로 (a) 설치용량만 50→100MW로 늘려도 계약용량 50MW 유지 시 매출·공급표가 불변함, (b) 계약용량을 80MW로 올리면(설치용량 100MW 이내) 공급표 ①이 정확히 80×6=480MWh로 늘어나고 매출도 비례 상승함, (c) 계약용량이 설치용량을 초과하면 경고 문구가 뜸을 각각 확인(콘솔 오류 없음).

**참고자료 출처 표기 제거 (2026-08-31)**: 사용자가 프로토타입·문서에서 "효성중공업 공급예상량 산정자료"를 참고했다는 문구를 전부 삭제해달라고 요청. 해당 자료를 근거로 도입했던 공급량 산정 ①~⑤ 단계 구조와 계약용량 개념 자체는 유지하되, 프로토타입 HTML 9곳과 두 문서에서 자료명·출처 문구만 제거하고 일반적인 설명으로 대체. 재검증 결과 기능 동작에는 변화 없음을 확인.

**당사 이익 목표 기준 적정단가 산정 추가 (2026-08-31)**: 사용자가 로컬에 저장한 `ESS 적정 입찰단가 프로토타입.html`(사업성모델 앱 폴더 직하)을 기준으로 기본값을 바꾸고, "당사 이익 100억원"을 기준으로 적정 입찰단가를 찾을 수 있도록(목표 IRR 역산과는 별개로) 정리해달라고 요청 — 이때 100억원이 6-1절의 어느 "당사 이익" 구성요소(세전/세후 순이익, 배당+회수 등)를 가리키는지도 입력할 수 있게 해달라고 함. 확인 결과 사용자가 지목한 로컬 파일은 claude.ai Artifact 뷰어의 바깥쪽 래퍼 페이지만 저장된 것이었고(실제 앱 콘텐츠는 iframe 안에 있어 브라우저 "다른 이름으로 저장"에 캡처되지 않음, `unitCount`·`analyzeEss` 등 앱 고유 문자열이 전혀 없음을 grep으로 확인), 가져올 수 있는 값이 없어 세션이 관리해 온 프로토타입 원본을 그대로 기준으로 작업을 진행했다. 구현: 결과 패널 상단에 "산정 기준"(당사 이익 목표 / 목표 IRR) 선택지를 추가하고, 당사 이익 목표 선택 시 "당사 이익 구성 기준"(누적 순이익 세전 / 누적 순이익 세후 / 누적 배당+최종회수) 드롭다운과 "목표 당사 이익(억원)" 입력을 노출. 기존 `solveBidPriceForTarget`(IRR 이분탐색)과 병렬로 `solveBidPriceForCompanyProfitTarget`(당사 이익 이분탐색, 가격이 오를수록 이익이 단조 증가한다는 전제)을 추가하고, `analyzeEss`가 `companyDividendPlusRecovery` 필드를 새로 반환하도록 확장. 기본 산정 기준을 "당사 이익 목표 — 100억원, 세전 순이익 기준"으로 변경(기존 "목표 IRR 8%"에서 전환). 헤드리스 브라우저로 (a) 기본값에서 33.32원/kWh로 역산되어 세전 순이익이 정확히 100.0억원이 됨, (b) 기준 지표를 세후로 바꾸면 33.44원/kWh·세후 정확히 100.0억원, (c) 배당+회수 기준으로 바꾸면 13.68원/kWh·배당+회수 정확히 100.0억원, (d) 목표를 900억원으로 올려도 42.49원/kWh로 정상 해를 찾음, (e) 목표를 50,000억원처럼 비현실적으로 높이면 "500원/kWh까지도 도달 못함" 메시지로 정상 실패 처리, (f) IRR 모드로 전환하면 기존 목표 IRR 8% 결과(41.68원/kWh)로 정확히 복귀함을 각각 확인(콘솔 오류 없음).

**실제 프로젝트 파이낸싱 참고자료 JSON 변환 및 반영 (2026-08-31)**: 사용자가 실제 프로젝트의 재무모델 참고 파일(사업성모델 앱 폴더 내 xlsm, 14개 시트·853KB)을 JSON으로 변환하고, 그 내용을 기준으로 프로토타입에 반영할 사항이 있으면 반영해달라고 요청. PowerShell + Excel COM으로 전체 14개 시트(Log/COVER/SENSITIVITY/ASSUMPTIONS/SUMMARY/TIC/REPAY/REV&OPEX/WATERFALL(Q)/FS DETAIL/IS(Y)/CF(Y)/BS(Y)/IRR(Q))의 비어있지 않은 모든 셀을 주소·값·수식과 함께 JSON으로 덤프(`ESS_베타테스트용.json`, 약 3.7MB, 55,000여 개 셀)해 같은 폴더에 저장 — 변환 스크립트 실행 중 `New-Object`를 `[System.IO.File]::WriteAllText`의 인자 자리에 인라인으로 못 쓰는 PowerShell 파서 오류가 한 번 발생해 별도 변수로 분리해 수정.

JSON을 읽어 우리 프로토타입과 대조한 핵심 발견:
- **ASSUMPTIONS 시트에 PCS계약용량/PCS설치용량/배터리계약용량/배터리설치용량이 명확히 분리되어 있어**, 직전에 반영한 "설치용량·계약용량 분리" 작업 방향이 실제 모델과 일치함을 재확인.
- **REV&OPEX 시트의 매출 산식**(`ESS방전량 = PCS계약용량 × 가동일수 × 24 × 가동률`, `발생수입 = 방전량 × 발전단가`)이 우리 `salesForRow`의 계산 구조와 정확히 동일함을 확인 — 별도 수정 불필요.
- **가동률 산식이 `IF(운영연차=1, 95%, IF(운영연차=2, 96%, 97%))`로 코드화**되어 있어, 기존에 단일 "기본 가동률" 값이던 것을 1년차/2년차/3년차 이후 3단계 규칙으로 반영.
- **DSCR 배당 임계치가 단순 1.15배/누적 1.30배**(참고자료는 우선주 조건 1.1/1.2도 갖고 있었으나 로그 기록상 우선주는 미사용으로 숨김 처리되어 있어 보통주 조건 채택), **할인율 4.5%**, **법인세가 2억/200억/3,000억 구간의 4단계**(기존 3단계 구조에 3,000억 초과 26.4% 구간 누락 확인)임을 확인해 각각 반영.
- **전력거래수수료가 방전량 × 0.1193원/kWh의 별도 변동비 항목**으로 존재함을 확인(공고의 "실제공급한 방전량 기준 전력거래수수료" 조항과 대응) — 우리 모델에 누락되어 있어 신규 변동 OPEX 항목으로 추가.
- **SENSITIVITY 시트에서 SPC 보통주 지분율이 KCH 20% / SI(전략적투자자) 30% / FI(재무적투자자) 50%의 컨소시엄 구조**임을 확인 — 지금까지 "당사(지분) 손익" 절대금액을 SPC 지분 100%로 계산해온 것이 실제와 다름을 발견. IRR(Q) 시트에도 KCH/SI/FI 각각 별도의 E-IRR 행이 있어 이 구조를 뒷받침.
- 반영하지 않은 구조적 차이: 참고자료의 선순위차입금은 목표 DSCR 기반 부채 사이징(원금불균등 상환) 방식인데 우리 모델은 여전히 고정 자기자본/타인자본 비율 방식만 지원 — 부채 규모 산정 알고리즘 자체를 바꾸는 큰 작업이라 이번 라운드에는 포함하지 않고 변수정의서 체크리스트에 기록.

구현: (1) 결과패널 5절에 "당사(KCH) 지분율"(기본 20%) 신규 입력 추가, `analyzeEss`가 당사 손익 4개 절대금액(출자금/배당+회수/순이익 세전·세후)에 `kchSharePct`를 곱하도록 수정(IRR·NPV·MOIC는 비율 불변이라 그대로 둠 — MOIC는 분자·분모 모두 지분율이 곱해지므로 수학적으로 상쇄됨을 이용). (2) 2절 가동률 UI를 "1년차 가동률"·"2년차 가동률"·"기본 가동률(3년차~)" 3개 입력으로 재구성하고 `syncOperatingRateRows`/`applyRateBtn`이 이 3단계 규칙으로 신규 행을 채우도록 수정. (3) 4절 OPEX에 "전력거래수수료(원/kWh)" 필드를 추가하고 `operatingCosts()`에 방전량×단가 변동비로 반영. (4) `TAX_BRACKETS`에 4번째 구간(3,000억 초과 26.4%) 추가, `ASSUMPTIONS.waccPct`를 7.5→4.5, `annualDscrThreshold`/`cumulativeDscrThreshold`를 1.1/1.2→1.15/1.3으로 변경, 세무·배당 프리셋 표시 텍스트도 동기화.

헤드리스 브라우저로 검증: 기본값(KCH 지분 20%)에서 목표 당사이익 100억원에 40.79원/kWh로 역산(세전 순이익 정확히 100.0억원), 지분율을 100%로 바꾸면 32.84원/kWh로 낮아짐(같은 100억원을 달성하는 데 필요한 총 프로젝트 이익이 줄어드므로 타당), 다시 20%로 복원하면 40.79원/kWh로 정확히 복귀. 가동률 그리드 기본값이 95/96/97/97…로 채워짐을 확인, 운영기간을 5년으로 줄여도 95/96/97/97/97로 재구성됨을 확인, 1년차 값을 수동으로 50%로 바꾼 뒤 "규칙 일괄 적용" 버튼을 누르면 95/96/97/97로 정확히 복원됨을 확인. IRR 모드로 전환하면 목표 IRR 8%가 새 WACC·DSCR·세율 하에서 41.08원/kWh·NPV 249.8억원으로 재계산됨(기존 41.68원/29.9억원에서 할인율 인하로 NPV가 크게 상승한 것은 타당). 모든 케이스에서 콘솔 오류 없음.

민감할 수 있는 사용자·회사 실명 정보(특정 프로젝트명 등)는 프로토타입 UI·공개 문서에 노출하지 않고 "실제 프로젝트 파이낸싱 참고자료"로만 일반화해 표기했다.

---

### 이터레이션 2 — 목표 성공: 반복 사용 가능 + ESS 부속 도구 통합

**목표**: "변수를 바꿔가며 반복 사용할 수 있고, ESS의 단가산정·가격환산까지 한 흐름에서 이어진다"를 증명한다. (기획문서 성공기준 "목표 성공"에 대응)

**범위**
- 발전원 4종 전체로 확장 (PV/ON/OF/ES 선택 + 차별 필드)
- 6.3 Sensitivity 분석 (매크로 변수 조합별, 최소 2축 매트릭스)
- 6.4 ESS 단가산정 모델 (공통 가격배율 + 목표 IRR 역산, 이분탐색으로 구현 — Excel Goal Seek을 대체)
- 6.5 ESS 가격환산 계산기 (이미 있는 JS 로직을 컴포넌트화해 이식)
- 6.4의 역산 판매단가 → 6.5의 입찰가격 기본값 자동 연결 (같은 페이지 내 상태 공유로 구현, 더 이상 외부 엑셀 링크에 의존하지 않음)
- What-if 반복: 입력 변경 시 전체 재계산, 결과 검토 후 재조정 루프 UX

**완료 조건 (DoD)**
- [ ] 4개 발전원 모두 프리셋 전환 시 필드·계산이 올바르게 바뀐다
- [x] Sensitivity 매트릭스가 축 2개 조합에서 정확히 계산된다 — **KCH 단가산정 모델의 가격배율×설비용량** 매트릭스(이전 로그)에 이어, **ESS 프로젝트 자체의 선순위 대출금리×OPEX 물가상승률** 2축 매트릭스도 추가 반영 완료(아래 로그 참조). 6.3이 원래 의도한 "매크로 변수 조합" 요구를 충족
- [ ] ESS 선택 시에만 "단가산정"·"가격환산" 탭이 노출된다 — 발전원 4종 확장 전이라 아직 해당 없음(현재 페이지 자체가 ESS 전용)
- [x] 단가산정에서 목표 IRR을 바꾸면 가격환산 탭의 입찰가격 기본값이 자동으로 갱신된다 — **6.4→6.5 직접 자동연결로 완료(아래 로그 참조).** 6.2(목표IRR/당사이익→적정단가)는 이터레이션 1부터 이미 6.5와 자동 연결되어 있었고(원/kWh 동일 단위라 문제 없음), 이번에 6.4(KCH 개발수수료, 억원 총액)도 6.5의 입찰가격에 원/kWh로 자동 환산해 더해지도록 신규 연결함
- [x] 이상치·가정 오류가 있으면 결과 화면에 플래그로 표시된다 — 전 섹션 전수 점검 완료, 11개 신규 경고 추가 반영(아래 로그 참조)

**산출물**: 4개 발전원 + ESS 부속 도구 2종이 통합된 다중 화면 프로토타입

**KCH 단가산정 모델 웹 이식 (2026-08-31)**: "이터레이션 2 진행해보자"는 사용자 요청에, 이터레이션 2의 5개 범위 항목(발전원 4종 확장/2축 Sensitivity/6.4 단가산정/6.5 가격환산/6.4↔6.5 연결) 중 어디부터 시작할지 먼저 확인한 결과 "ESS 단가산정 모델 웹 이식"을 선택받음. `ESS_단가산정모델.xlsx`(엑셀, 4개 시트: 읽어보기/입력/현금흐름/요약)를 PowerShell+Excel COM으로 JSON 덤프해 모든 셀의 값·수식을 확보한 뒤, 그 수식을 그대로 JS로 이식:
- 현금흐름 로직(`kchBuildCashflow`): 연차별 개발/공사/운영 단계 판정 → 4개 수익원(①부지임대료-연간·②사전개발비-COD 1회·③공동접속료-연간 또는 일시금 선택·④운영수익-연간, 각각 공통 가격배율 적용) − KCH 비용(용지비 1회+개발인허가비 연간+자체관리비 연간) = 순현금흐름. 엑셀의 정확한 조건식(`IF(Phase="운영", ...)`, `IF(연차=(개발+공사기간-1), ...)` COD 시점 판정 등)을 그대로 옮김.
- `kchSolveScaleFactor`: 기존 `irr()`/`npv()` 헬퍼를 재사용해 "공통 가격배율 → 목표 IRR" 이분탐색(Excel Goal Seek 대체, 이터레이션 1의 `solveBidPriceForTarget`과 동일 패턴).
- 가격배율(0.6~1.6, 11단) × 설비용량(20~80MW, 7단) 2축 민감도표, 목표 IRR 이상 칸 초록 강조 — 엑셀의 Data Table을 그대로 재현.
- 골든마스터 검증(엑셀 원본 값과 대조): 기본 입력값에서 공통 가격배율 0.2783배(엑셀 0.27828, 소수 첫 4자리 일치) · 달성 IRR 9.00%(엑셀 9.00183% — 엑셀 Goal Seek은 기본 허용오차 안에서 근사 수렴하는 반면 이분탐색 60회 반복은 사실상 완전 수렴이라, 산출 단가에 0.01% 내외 미세한 차이가 남는 것은 정상) · NPV 1.57억원(엑셀 1.57223 일치) · 총수익 38.31억원(엑셀 38.3107 일치) · KCH총비용 23.50억원(엑셀 23.5 완전 일치) · 민감도표 1.0배 행 값(17.0%/28.7%/39.8%/50.8%/61.7%/72.7%/83.8%)이 엑셀 원본과 소수 첫째 자리까지 정확히 일치함을 확인. 목표 IRR을 12%로 올리면 가격배율이 0.3214배로 상승, 500%처럼 비현실적인 목표를 주면 "가격배율 5.00배까지도 도달 못함" 오류 배너로 정상 실패 처리됨을 확인. 기존 ESS 입찰단가 계산부는 전역 이벤트 배선에서 `kch` 접두사 필드를 명시적으로 제외해 서로 간섭하지 않음(회귀 테스트로 기존 결과 40.79원/kWh 불변 확인). 콘솔 오류 없음.
- 이 절은 ESS 입찰단가 계산부와 완전히 독립적인 별도 시나리오로 구현했다 — 설비용량 등 사업 정보를 각자 따로 입력해야 한다는 한계를 확정필요 항목에 기록했고, 6.4→6.5 자동 연결(범위 항목)이 실제로는 가격 단위 체계가 달라 성립하지 않는다는 점도 위 DoD 표에 기록했다.

**ESS 본체 Sensitivity 매트릭스 추가 (2026-08-31)**: 이터레이션 2 남은 두 항목(발전원 4종 확장 / ESS 본체 2축 Sensitivity) 중 어느 것부터 이어갈지 재확인한 결과, "발전원 4종 확장은 다른 인원이 진행 중이니 Sensitivity를 먼저 해달라"는 요청을 받아 진행 방향을 전환. 좌측 "단가산정모델"에서 계산된 현재 적정 입찰단가를 고정한 채, **선순위 대출금리 × OPEX 물가상승률**을 각각 현재 입력값 대비 −1.5%p~+1.5%p(0.5%p 간격, 7단)로 편차(Δ)를 주는 7×7 매트릭스를 추가:
- 처음에는 P-IRR(프로젝트 IRR)로 구현했는데, 테스트 중 "대출금리가 오를수록 오히려 P-IRR이 상승"하는 역설적인 결과를 발견. 원인 확인: 본 모델의 `projectFlowPreTax`는 EBITDA와 CAPEX만 반영하고 원리금 상환 자체는 차감하지 않으며, 법인세만 이자비용의 절세효과를 통해 간접 반영하는 구조라, 대출금리 상승 → 이자비용 증가 → 법인세 절감 → 세후 프로젝트 현금흐름 증가로 이어지는 것이었다(계산 자체는 정확했지만 "대출금리 민감도"라는 취지에는 반직관적이고 부적합한 지표). 지분 IRR(E-IRR, 배당세전)은 원리금 상환을 직접 차감하므로 대출금리에 직관적으로 반응해, 지표를 **E-IRR**로 교체하고 그 이유를 화면 설명문에 그대로 남겼다.
- 기준(Δ=0,0) 셀은 상단 결과 패널의 "달성 E-IRR(배당세전)" KPI와 정확히 일치하도록 구현(회귀 검증 완료)하고 시각적으로 테두리 강조, 기준 대비 E-IRR이 유지·개선되는 칸을 초록으로 강조.
- 헤드리스 브라우저로 검증: 기본값에서 중앙(0,0) 셀 7.45%가 "달성 E-IRR" KPI(7.45%)와 정확히 일치, 대출금리 −1.5%p 행은 8.32~9.22%로 전부 기준보다 높고 +1.5%p 행은 5.70~6.55%로 전부 기준보다 낮아 단조적으로 직관적인 방향성을 보임, OPEX 물가상승률도 같은 행 안에서 오른쪽으로 갈수록(상승률 증가) 단조 감소함을 확인. 선순위 대출금리 입력을 5.2%→6.5%로 바꾸면 매트릭스 기준값도 즉시 6.50%로 재중심화됨을 확인. 준공지연 3단계(계약 해지)처럼 상단 결과가 "N/A"인 상태에서는 매트릭스도 안내 문구로 정상적으로 비워짐을 확인. 콘솔 오류 없음.

**실제 장비 규모 기본값 반영 및 CAPEX·OPEX 대수 연동 제거 (2026-08-31)**: 사용자가 이전에 확인한 실제 프로젝트의 설치·계약용량 수치(설치 99.75MW=3.99MW×25대, 732.526MWh≈4.883MWh×150대 / 계약 96MW·576MWh=계약용량×6배수)를 프로토타입 1절·2절의 기본값으로 반영해달라고 요청. 반영 과정에서 CAPEX·OPEX 계산의 구조적 결함을 발견해 함께 수정:
- 기존에는 "배터리 유닛 대수"가 1(대략적 표현으로 시스템 전체를 1유닛으로 취급)일 때만 CAPEX·OPEX 항목별 금액(1대 기준)×대수 방식이 안전했는데, 이제 배터리 유닛 대수가 실제 배터리랙 개수(150대), PCS 대수가 실제 PCS 개수(25대)를 나타내게 되면서 그대로 곱하면 CAPEX 총액이 수만억 원대로 폭증하는 문제가 발생. 이에 CAPEX·OPEX 13/4개 항목 각각을 "대수 × 1대 단가" 대신 **프로젝트 총액 직접 입력**으로 전환(`readModel`에서 `* unitCount` 곱셈 제거), 관련 UI 라벨("배터리 유닛 1대 기준" → "프로젝트 총액 직접 입력")도 갱신.
- 1절: 배터리 유닛 대수 1→150, 대당 저장용량 200→4.883MWh(설치 저장용량 732.5MWh), PCS 대수 1→25, 대당 PCS 출력 50→3.99MW(설치 출력용량 99.75MW), 사업명 "ESS 50MW/200MWh" → "ESS 96MW/576MWh"로 갱신.
- 2절: 계약용량 기본값 50→96MW(계약 저장용량 576MWh = 96×6h는 기존 ①공식으로 자동 도출, 별도 필드 불필요).
- 헤드리스 브라우저로 검증: 설치 저장용량 732.5MWh·설치 출력용량 99.8MW(반올림)·저장시간 7.34h·계약용량 96MW가 모두 정확히 표시됨, 계약용량이 설치용량 이내라 경고 없음, CAPEX/OPEX 합계가 여전히 1,005.0억원/12.5억원(대수 곱셈 제거로 예전 1대 기준 값과 동일 — 폭증하지 않음)을 확인, 공급량 산정표 ①이 정확히 576.0(=96×6)으로 계산됨, 전체 결과(적정 입찰단가·P-IRR 등)가 새 규모에 맞게 재계산됨(콘솔 오류 없음). CAPEX·OPEX 각 항목 금액 자체는 여전히 이전 50MW급 예시 규모의 초안값이라 96MW급 실제 견적으로 교체가 필요하다는 점을 변수정의서 체크리스트에 남겼다.

**EPC 총액 수기입력 옵션 + 대출금리×EPC총액 Sensitivity (2026-08-31)**: 사용자가 "EPC 기자재별 단가를 13개나 입력하는 게 번거로우니 EPC 총액을 수기로 입력하는 것도 가능하게 하고, EPC 단가 Sensitivity도 대출금리 시나리오와 같은 방식으로 반영해달라"고 요청.
- 3절 CAPEX에 "CAPEX 입력 방식" 선택지(항목별 입력 / EPC 총액 직접 입력)를 추가. 재무엔진(`readModel`)은 CAPEX 항목 배열의 합계만 사용하므로, 총액 모드에서는 `{id:'epcLumpSum', label:'EPC 총액(수기입력)', value: 입력값}` 단일 합성 항목 하나로 대체해도 계산이 동일함을 이용. 모드 전환 시 직전 항목별 합계를 총액 입력란 기본값으로 채워 넣어(`itemizedSum.toFixed(1)`) 전환 순간 값이 끊기지 않게 함.
- Sensitivity 절에 두 번째 매트릭스 "대출금리 × EPC 총액"(대출금리 Δ ±1.5%p, EPC 총액 ±15%, 7×7)을 첫 번째 "대출금리 × OPEX 물가상승률" 매트릭스와 동일한 구조·강조 규칙(기준 대비 E-IRR 유지·개선 시 초록, 기준 셀 테두리)으로 추가.
- **테스트 중 버그 발견 및 수정**: 모드 전환 시 총액 입력란에 `fmt()`(천단위 콤마 포맷터, 예: "1,005.0")를 그대로 대입했더니, 콤마가 포함된 문자열은 `type="number"` input에서 브라우저가 거부해 값이 통째로 빈 문자열로 리셋되는 문제가 발생 — CAPEX 총액이 0으로 읽혀 결과가 왜곡됐다(적정 입찰단가가 22.51원→10.64원으로 급락). `fmt()` 대신 순수 숫자 문자열(`itemizedSum.toFixed(1)`)로 교체해 수정. 이 참에 파일 전체에서 `.value = fmt(...)` 패턴을 모두 점검해, 다른 모든 자리는 `type="number"`가 없는(기본 텍스트) 비활성 표시 필드라 콤마가 있어도 문제없음을 확인 — 유사한 잠재 결함은 이 한 곳뿐이었음.
- 헤드리스 브라우저로 재검증: 항목별→총액 모드 전환 시 총액이 정확히 1005.0으로 채워지고 결과(22.51원)가 그대로 유지됨, 총액을 1500으로 직접 수정하면 결과가 28.35원으로 상승, 총액→항목별로 되돌리면 1,005.0억원·22.51원으로 정확히 복원됨을 확인. EPC 매트릭스는 기준(0,0) 셀이 "달성 E-IRR" KPI(7.45%)와 정확히 일치, EPC 총액이 늘어날수록·대출금리가 오를수록 E-IRR이 단조 감소하는 직관적인 방향성을 확인. 콘솔 오류 없음.

**KCH 개발수수료 단위 통일 — 원/kW 단가 폐지, 억원 총액으로 전환 (2026-08-31)**: 사용자가 "전체적으로 단위가 안 맞는 것 같다, 예를 들어 사전개발비가 150억 기준으로 보이는데 15,000원/kW로 표시되어 있다"고 지적하며, 총액 기준으로 억원 단위로 전체 통일해달라고 요청. 원인: `ESS_단가산정모델.xlsx`를 이식할 때 원본 그대로 "원/kW", "원/kW/년" 단가 × 설비용량(kW)으로 총액을 환산하는 구조를 그대로 옮겼는데, 이 단위가 다른 절(CAPEX·OPEX·KCH 비용 등은 전부 억원 총액)과 이질적이라 헷갈린다는 지적이었다.
- 4개 수익원(①부지임대료·②사전개발비·③공동접속료·④운영수익) 입력을 "원/kW(/년)" 단가에서 **"억원(/년)" 총액**으로 전환하고, `kchBuildCashflow`에서 `capacityMW × 1000 × 단가 / 1e8` 환산식을 제거 — 입력한 총액에 가격배율만 곱하는 단순한 구조로 바뀜. 기본값도 예전 단가×기준설비용량(50MW) 환산값과 수학적으로 동일하게(예: 사전개발비 15,000원/kW×50,000kW/1e8=7.5억원) 재설정해 결과값이 그대로 보존되도록 함.
- 부작용 처리: 기존 "가격배율×설비용량" 민감도표는 설비용량 열을 바꿔가며 원/kW 단가에 곱해 총액을 재계산하는 방식이었는데, 총액을 직접 입력하는 구조로 바뀌면서 설비용량을 바꿔도 더 이상 계산에 영향을 주지 않아 표가 무의미해지는 부작용이 있었다. 이를 "입력한 총액은 현재 설비용량 기준이며, 다른 설비용량을 가정할 때는 총액을 (가정 설비용량÷현재 설비용량) 비율로 비례 스케일링한다"는 보조 인자(`revenueCapacityScale`)로 해결해, 예전과 수학적으로 동일한 민감도표 결과를 유지.
- 헤드리스 브라우저로 검증: 새 억원 기준 기본값에서 공통 가격배율 0.2783배·달성 IRR 9.00%·NPV 1.57억원·총수익 38.31억원·KCH총비용 23.50억원 — 이전 원/kW 기준 골든마스터와 완전히 동일한 값이 나옴을 확인(수학적으로 동치인 재구성임을 재확인). 산출 총액(억원)도 이전 산출 단가(원/kW)를 설비용량으로 환산한 값과 정확히 일치(예: 사전개발비 2.09억원 = 이전 4,173.8원/kW×50,000kW/1e8). 민감도표 1.0배 행도 이전과 동일한 17.0%/28.7%/39.8%/50.8%/61.7%/72.7%/83.8%를 그대로 재현함을 확인. 사전개발비를 7.5→10억원으로 늘리면 가격배율이 0.2783→0.2694배로 낮아짐(더 큰 기준총액에 곱하니 목표 IRR 달성에 필요한 배율이 작아짐 — 타당). 콘솔 오류 없음.

**6.4→6.5 직접 자동연결 (KCH 개발수수료 → 가격환산계산기 입찰가격, 2026-09-03)**: "6.4↔6.5 자동연결" DoD 재해석 방향을 확인한 결과, 기존 6.2 경유 재해석이나 계약용량 필드 통합이 아니라 **"6.4→6.5 직접 자동연결 신설"**을 선택받음. 두 절이 서로 다른 성격의 수익(SPC 지분배당 vs KCH 개발자 수수료)이라 무작정 값을 합치면 안 된다는 기존 설계 원칙을 지키기 위해, 반영 방식을 다시 확인한 결과 **"6.2 솔브 값에 자동 가산"**(추천안)을 선택받아 구현:
- **환산 공식**: KCH 4절의 "생애주기 누적 총 수취액"(억원)을 <strong>이 ESS 프로젝트 자체</strong>(KCH 절의 자체 설비용량이 아니라 2절 계약용량·가동률·운영기간 기준)의 생애주기 총 발전량(kWh)으로 나눠 "이 수수료를 회수하려면 입찰단가에 얼마를 더 얹어야 하는가"를 원/kWh로 환산(`totalLifetimeGenerationKWh(model)` 신규 함수 — `salesForRow`와 동일한 발전량 산식을 연도별로 합산). 이 환산분을 6.2가 솔브한 "순수" 적정 입찰단가에 더해서만 6.5(가격환산계산기)의 입찰가격 기본값에 반영 — 6.2 자신의 결과(heroBidPrice·bidPriceDisplay)와 P-IRR·NPV·당사이익 등 SPC 자체 재무지표는 전혀 건드리지 않음(더한 값을 다시 `analyzeEss`에 넣지 않으므로 순환 영향 없음).
- 6.5 절에 "단가산정모델 적정 입찰단가(원값)"·"KCH 수수료 원/kWh 환산 반영분" 두 계산 필드를 신규 노출해 두 값이 각각 얼마인지, 왜 최종 입찰가격이 그 합인지 투명하게 보여줌.
- **양방향 실시간 연결**: 기존에는 메인 ESS 입력 필드만 `recalcAll()`을, KCH 필드만 `kchRecalc()`을 각각 독립적으로 트리거했는데(서로 간섭 없음), 이제 `kchRecalc()` 끝에 `recalcAll()` 호출을 추가해 KCH 필드를 바꿔도 즉시 6.5가 갱신되도록 연결(반대 방향은 이미 `recalcAll()`이 매번 최신 `kchLifetimeTotalRaw`를 읽으므로 자동으로 성립 — `recalcAll()`은 `kchRecalc()`을 호출하지 않으므로 순환 재귀 없음).
- 헤드리스 브라우저로 검증: 기본값에서 6.2 순수 결과 22.51원/kWh(불변 확인용 `bidPriceBaseDisplay`도 22.51), KCH 환산분 +1.13원/kWh, 6.5 입찰가격 23.63원/kWh(=22.51+1.13, 반올림 오차 범위 내 일치)를 확인. KCH 필드(①부지임대료 1.5→3.0억원/년)만 바꾸면 heroBidPrice·kpiPIrr(SPC 자체 지표) **완전히 불변**인 채로 KCH 환산분만 +1.13→+1.34원/kWh로, 6.5 입찰가격만 23.63→23.85원/kWh로 정확히 갱신됨을 확인(두 수익 개념이 실제로 분리돼 있음을 재확인). 반대로 메인 ESS 계약용량을 96→48MW로 절반으로 줄이면(발전량도 절반) KCH 환산분이 +1.34→+2.68원/kWh로 정확히 2배가 됨을 확인(발전량 반비례 공식이 정확함). 준공지연 3단계(계약 해지)로 전환하면 새로 추가한 두 필드와 6.5 입찰가격이 모두 빈 값으로 정상 초기화됨을 확인. 전 과정 콘솔 오류·예외 0건.

**이상치·가정 오류 플래그 전수점검 (2026-09-03)**: "이상치 플래그 전수점검 실시" 요청에 따라 전 5개 섹션(사업개요~금융세무)·KCH 절을 계산 로직 기준으로 훑어, 값이 비정상 범위일 때 결과에 영향을 주지만 아무 경고 없이 조용히 처리(clamp)되거나 계산이 왜곡되는 지점을 찾아 11개 경고를 신규 반영(`readModel()`에 전부 위치, 기존 `calendarWarn`/`contractCapacityWarn`과 동일한 `.warn`/`.warn show` 토글 패턴):
- **재무모델이 실제로 붕괴하는 케이스(우선순위 최상)**: 자기자본 비율+주민참여채권 비율 합계가 100% 초과 시 `solveInvestment()`의 `senior = total - equity - bond`가 음수가 되는 것을 발견 → `capitalStructureWarn` 추가. 원금 거치기간이 선순위 상환기간 이상이면 `levelDebtService()`의 상환기간이 0 이하가 돼 정상상환 없이 사실상 전액 만기일시상환(발룬)으로 바뀌는 것을 발견 → `seniorTermWarn` 추가.
- **공고 규정 위반(계산은 되지만 실무상 무효)**: 계약용량이 공고 Ⅰ.가·Ⅳ.가 기준 신고범위(10MW 초과~100MW 미만) 밖인데도 경고가 없던 것을 발견 → `contractCapacityRangeWarn` 추가(기존 `contractCapacityWarn`은 "설치용량 초과"만 검사하는 별도 항목이라 유지하고 신규 병설).
- **조용한 clamp로 사용자가 모를 수 있는 케이스**: ① 공사기간이 길거나 COD가 일러 착공연도가 사업 시작연도보다 앞서면 `developmentYears`가 `Math.max(0, ...)`로 조용히 0년으로 조정되는 것을 발견 → `developmentTimelineWarn` 추가. ② 연평균 미이행률이 0~100% 밖이면 `Math.min(100, Math.max(0, ...))`로 조용히 clamp되는 것을 발견 → `nonComplianceRangeWarn` 추가.
- **물리적으로 불가능한 값**: 운영연차별 가동률이 0~100% 밖(예: 150%)이면 발전량이 물리적 상한을 넘는데 경고가 없던 것을 발견 → `opRateRangeWarn` 추가. 당사(KCH) 지분율은 HTML `min="0" max="100"`가 있지만 JS단에서 강제되지 않아 150% 등도 그대로 입력·계산되던 것을 발견 → `kchShareRangeWarn` 추가.
- **입력 실수 방지**: CAPEX·OPEX 항목에 음수 값이 들어가도 합계에 그대로 반영되던 것을 발견 → `capexNegativeWarn`/`opexNegativeWarn` 추가(어떤 항목이 음수인지 라벨까지 명시).
- 헤드리스 브라우저로 11개 항목 각각 개별 트리거→경고 노출 확인→값 원복→경고 정상 해제까지 전수 검증(예: 자기자본70%+주민채권40%=110%→경고 노출 후 30%/5%로 복원 시 정상 해제, 원금거치10년=상환기간10년→경고, 계약용량 5MW→규정범위 경고, 공사기간 50년→개발기간 강제조정 경고, CAPEX 배터리 항목 -10억원→경고 등). 정상 기본값 상태에서는 신규 경고 11개 전부 숨김 상태이고 기존 화면 레이아웃에 변화가 없음을 확인, 이상치를 만들었다가 복원하는 전 과정에서 콘솔 오류·예외 0건.

---

### 이터레이션 3 — 이상적 성공: 팀이 함께 쓰는 형태

**목표**: "여러 프로젝트(시나리오)를 저장·비교할 수 있고, 결과를 부서와 공유할 수 있다"를 증명한다. (기획문서 성공기준 "이상적 성공"에 대응)

**범위**
- 시나리오 저장/불러오기/비교 (localStorage 기반, 여러 프로젝트를 나란히 비교하는 표)
- PDF 리포트 자동 생성(투자지표 + 재무제표 + Sensitivity 요약) 및 다운로드
- "추가 검토 필요사항"(4장의 발전원별 모델링 한계) 결과 화면에 상시 노출
- 반응형·접근성 마무리 (모바일 폭 대응, 스크린리더 라벨 전수 점검)
- 사용성 다듬기: 입력 검증 메시지 구체화, 로딩/에러 상태 처리

**완료 조건 (DoD)**
- [x] 시나리오 3개 이상을 저장하고 한 화면에서 비교할 수 있다 — localStorage 기반 저장·불러오기·삭제·비교표 구현 완료(아래 로그 참조)
- [ ] PDF 다운로드 결과물이 화면 표시 내용과 일치한다 — 미착수. 대신 CSV 다운로드를 Assumption(입력값)·Summary(핵심지표)·Appendix(연도별 현금흐름 상세, 조달내역·손익계산서 재구성·감가상각/부채상환 스케줄 포함) 3절 구조로, 그리고 시나리오별 전체 입력모델을 담은 개별 `.json` 다운로드까지 반영(아래 로그 참조) — 검증 목적은 이 두 다운로드로 상당 부분 충족되어, PDF는 우선순위가 낮아짐
- [x] 발전원별 모델링 한계 안내가 해당 발전원 선택 시 항상 보인다 — ESS 결과 카드에 상시노출 완료(아래 로그 참조). 발전원 4종 확장 후 PV/ON/OF에도 동일 패턴 적용 필요
- [x] 좁은 화면(768px 이하)에서 레이아웃이 깨지지 않는다 — 768/480/375px 모두 가로 스크롤 없이 정상 렌더링 확인(아래 로그 참조)

**산출물**: 부서 공유 가능한 완성형 프로토타입

**시나리오 저장·비교 (localStorage) + CSV 다운로드 (2026-08-31)**: "이제 3단계 진행해볼래?"라는 요청에 이터레이션 3의 범위 항목 중 어디부터 시작할지 확인한 결과 "시나리오 저장/비교 (localStorage)"를 선택받음. 진행 중 "엑셀 다운로드 버튼을 만들어서 실제 자료가 맞는지 다운로드 및 검사할 수 있도록 해달라"는 요청이 추가로 들어와 같은 라운드에서 함께 반영:
- 시나리오 저장·비교 UI를 헤더 바로 아래(모든 섹션 위) 독립 카드로 추가. "현재값 저장" 클릭 시 `captureScenarioState()`가 `.wrap` 내 모든 input/select(비활성 필드·시나리오 이름 입력란 제외) + 동적 CAPEX/OPEX/가동률 행 값을 통째로 캡처하고, `captureKpiSnapshot()`이 핵심 KPI 17종(적정 입찰단가·P-IRR·E-IRR 세전후·NPV·총투자비·CAPEX/OPEX 합계·당사 누적 출자금/배당/순이익/MOIC 등)을 함께 저장 — 이름·저장시각과 함께 `essBidPriceScenarios.v1` localStorage 키에 배열로 누적.
- 저장된 시나리오는 목록(이름·저장시각·적정 입찰단가·P-IRR 요약)과 비교표(시나리오를 열로, 17개 지표를 행으로 나열)로 동시에 렌더링. "불러오기" 클릭 시 `applyScenarioState()`가 저장된 모든 필드값을 복원한 뒤, 값을 건드리지 않는 순수 가시성 동기화 함수(`syncSolveModeVisibility()`/`syncCapexModeVisibility()` — 기존 입력 이벤트 핸들러에서 부수효과와 분리해 새로 추출)만 호출하고 전체 재계산(`recalcAll(); kchRecalc();`)하는 구조. "삭제"·"전체 삭제"도 지원. localStorage는 이 브라우저에만 저장되고 다른 기기·뷰어와 공유되지 않는다는 점을 화면에 명시.
- CSV 다운로드: Artifact `downloads` capability(`capabilities:{downloads:true}`로 선언, `window.claude.use('downloads')`)를 사용해 비교표 전체를 UTF-8 BOM 포함 CSV로 내보냄(`downloads.save({filename, data})`). 이 뷰어의 다운로드 허용 확장자 목록에 `.xlsx`가 없어(csv·docx·pdf 등은 있으나 엑셀 바이너리 형식은 불가) 네이티브 엑셀 파일이 아닌 `.csv`로 제공하며, 화면에 "Excel에서 열기" 안내 문구로 이 제약을 명시. `downloadsApi`가 없는 뷰어 환경(구버전 뷰어 등)에서는 버튼 클릭 시 경고 배너로 안내하고 조용히 실패하지 않도록 처리.
- 헤드리스 브라우저로 검증(시나리오 3개 저장→비교표 렌더→저장 1 재로드→시나리오 2 삭제→전체 삭제, 총 2회 라운드로 로컬스토리지 오염 없이 재검증): 저장 직후 목록·비교표에 3개 시나리오가 정확한 이름·적정 입찰단가(22.51/27.25/33.73원)·P-IRR로 반영됨을 확인. 계약용량을 50MW로 임의로 바꾼 뒤 저장 시나리오 1(96MW 기본안)을 불러오면 계약용량 96MW·산정기준(당사이익목표)·CAPEX 입력방식(항목별)·적정 입찰단가 22.51원까지 전부 정확히 복원됨을 확인(가시성 동기화 함수가 값 복원과 독립적으로 잘 작동). 시나리오 삭제·전체 삭제 후 개수가 각각 정확히 감소함을 확인. `window.claude`가 없는 헤드리스 환경에서 CSV 버튼 클릭 시 "이 화면에서는 다운로드 기능을 사용할 수 없습니다..." 경고가 뜨고 크래시하지 않음을 확인. `Page.addScriptToEvaluateOnNewDocument`로 `window.claude.use('downloads')`를 실제 계약대로 스텁해 재검증한 결과, `downloads.save()`가 정확히 1회 호출되고 파일명(`ESS_시나리오_비교_2026-08-31.csv`)·BOM(`﻿`)·콤마 포함 필드의 CSV 이스케이프(`"설치용량(MW, 참고)"`처럼 따옴표로 감쌈)·전체 19행(헤더+저장시각+17개 지표)이 모두 기대대로임을 확인. 두 라운드 모두 콘솔 오류·예외 0건.
- 자체 발견 및 수정: 초기 구현에서 페이지 최초 로드 시 `renderScenarioList()` 호출이 누락되어 저장된 시나리오가 없어도 "저장된 시나리오가 없습니다" 안내 문구가 뜨지 않는(빈 화면으로 보이는) 결함을 헤드리스 테스트로 발견 — IIFE 마지막 `recalcAll(); kchRecalc();` 뒤에 `renderScenarioList();` 호출을 추가해 수정, 재검증 완료.

**CSV 내보내기를 Appendix·Assumption·Summary 3절 구조로 확장 (2026-08-31)**: "지금 CSV 다운로드를 하면 최종값만 나오는데, Appendix부터 Assumption, Summary 등이 모두 포함된 함수값이 반영된 엑셀을 받을 수 있도록 해야한다(검증이 가능하도록)"는 요청. 플랫폼 `downloads` capability의 허용 확장자에 `.xlsx`(다중 시트)가 없어 네이티브 엑셀 파일은 낼 수 없으므로, 대신 **절 구분선을 넣은 단일 CSV 한 장에 3개 절을 순서대로 담는 방식**으로 구현:
- **엔진 변경**: `analyzeEss(model)`가 그동안 요약 KPI만 반환하고 연도별 중간값(매출·OPEX·감가상각·원리금상환·DSCR·법인세·배당 등)은 `cal.rows`에 잠깐 담았다가 버리던 구조를, 연도별 루프에서 계산되는 모든 중간값을 `detailRows` 배열로 함께 반환하도록 확장(`detail`, 39개 컬럼) — 요약 KPI 계산 로직 자체는 전혀 건드리지 않고 이미 계산되던 값을 "버리지 않고 담아서 반환"만 추가한 것이라 회귀 위험이 낮음. 아울러 총투자비 조달내역(`funding`: 자기자본/선순위/주민채권/금융수수료/DSRA/건설이자)도 함께 반환.
- **DRY 리팩터**: `recalcAll()`에 있던 "현재 화면 입력값 → 솔브 모드별 적정 입찰단가 역산" 분기를 `solveCurrentModel(model)` 함수로 추출해, 시나리오 저장 시점에도 동일 로직으로 재계산(`captureDetailSnapshot()`)하도록 함 — 화면에 보이는 값과 CSV Appendix 값이 절대 어긋나지 않도록 로직을 하나로 합침.
- **Assumption 절**: `.field` 요소의 `<label>` 텍스트를 그대로 읽어(`buildFieldLabelMap()`) 사람이 읽는 라벨로 모든 입력 필드(비활성 계산값 표시 필드 포함, 예: 설치 저장용량·저장시간 등 "함수값") + CAPEX 13항목·OPEX 4항목·운영연차별 가동률을 시나리오별 열로 나열.
- **Appendix 절**: 시나리오마다 "총투자비 조달내역" 요약 + 연도별 현금흐름 상세표(연도·단계·가동률·발전량·매출·OPEX·EBITDA·CAPEX집행·조달내역·감가상각·선순위/채권 이자·원금·잔액·DSCR(연간/누적)·법인세·배당·최종회수·프로젝트/지분/당사지분 현금흐름 세전후 등 39개 컬럼)을 그대로 출력.
- 헤드리스 브라우저로 검증: 3개 절이 항상 Assumption→Summary→Appendix 순서로 나타남, 일반 입력값·CAPEX 13항목·OPEX 4항목·운영연차별 가동률이 시나리오별로 올바른 값으로 채워짐, Appendix의 총투자비(1,130.0억원)가 화면 KPI와 일치, 연도별 표에서 DSCR 게이트("Y"/"N")·이익준비금 잔여액이 매년 누적 증가(1.071→3.286→5.884→8.052억원)하는 등 재무적으로 타당한 흐름을 확인, 콤마 포함 필드("설치용량(MW, 참고)" 등)가 CSV 인용부호로 정확히 이스케이프됨. 콘솔 오류 0건.

**DSCR 게이트·이익준비금 잔여액 산정 근거를 Appendix에 노출 (2026-08-31)**: "단순 DSCR 기본수치(연간 1.15, 누적 1.30), 이익준비금 잔여액 반영 등 배당가능재원 기준을 고려한 최적단가가 산정되어야 하는데 그 내용 반영이 필요"라는 요청. 확인 결과 **배당 로직 자체(`analyzeEss` 내 DSCR 게이트·법정준비금 적립·잔여액 상한)는 이미 정상 반영되어 있었고**(연간·누적 DSCR이 각각 1.15배·1.30배 미만이면 배당을 유보하고, 배당가능액의 10%를 자기자본 50% 한도까지 이익준비금으로 적립한 뒤 나머지만 배당 — 이 배당액이 그대로 지분 현금흐름에 들어가 IRR/당사이익 역산에 이미 반영됨), 실제 문제는 **그 계산 과정이 화면 어디에서도 검증 가능한 형태로 노출되지 않아 신뢰하기 어려웠다는 점**이었다. 위 CSV Appendix 확장 작업에 다음 컬럼을 추가해 해결: DSCR(연간)·DSCR(연간) 기준 충족(Y/N)·DSCR(누적)·DSCR(누적) 기준 충족(Y/N)·배당가능 현금(원리금상환 후)·배당가능재원 상한(감가상각·이자·법인세 차감후)·이익준비금 당해 적립액·**이익준비금 잔여액(기말)**. 헤드리스 브라우저로 검증: 게이트 미달 연도가 있으면 해당 연도 배당이 0으로 유보되고 다음 연도로 이월됨, 이익준비금 잔여액이 매년 적립분만큼 정확히 누적되다가 자기자본의 50% 상한에 도달하면 적립이 멈춤, 최종연도에 DSRA+이익준비금 잔여액+유보현금이 전액 "최종연도 회수"로 지분 현금흐름에 반영됨을 확인.

**KCH 개발수수료 단가산정 — "총액 직접 입력" 모드 신설 및 상단 목표수익 연동 (2026-08-31)**: "KCH 개발수수료 단가산정 부분이 상단 목표수익과 연계될 수 있도록 해야 할 것 같다(현재는 보기도 불편하고 연계도 안 되고 있음). 임대료 30억·공동접속료 10억·사전개발비 60억·기타 0원으로 총 100억원을 수취할 때 기준단가가 나올 수 있도록 해달라"는 요청. 연간 항목(①임대료·④운영수익, 억원/년)과 1회성 항목(②사전개발비·③공동접속료, 억원)을 합산할 때 어떻게 환산할지 사용자에게 확인한 결과 **"운영기간 전체 누적 총액으로 환산 후 합산"**(연간 항목 × 운영기간·상승률 반영 후 1회성 항목과 합산 — 상단 "목표 당사 이익"이 프로젝트 생애 전체 누적 세전순이익이라는 것과 개념을 맞춤)을 선택받아 반영:
- 4절에 산정 방식 선택지 "총액 직접 입력(권장)" / "목표 IRR 역산(레거시)"을 추가. 새 기본 모드인 총액 직접 입력에서는 공통 가격배율을 1로 고정하고 4개 수익원 입력값을 그대로 실수취액으로 사용(목표 IRR 입력·가격배율 결과 행은 숨김). 기존 목표 IRR 역산 방식은 레거시 옵션으로 완전히 보존(회귀 검증 완료 — 아래 참조).
- "생애주기 누적 총 수취액" KPI를 신설 — 이미 있던 `kchBuildCashflow()`의 연도별 `revenue`(4개 수익원의 연간 상승률·1회성 시점 반영이 이미 정확히 구현되어 있었음)를 그대로 합산해(`sum(rows.map(r=>r.revenue))`) 표시. 이 값을 "▶ 이 총액을 상단 '목표 당사 이익'에 반영" 버튼으로 상단 단가산정모델의 `targetCompanyProfit`에 밀어넣고 산정 기준을 "당사 이익 목표"로 전환한 뒤 재계산 — 클릭할 때만 동작하는 명시적 연결이며 자동 동기화는 하지 않음(두 수익은 SPC 지분배당 vs KCH 개발자 수수료로 발생 근거가 다른 별도 항목이라는 점을 화면 설명문에 명시).
- 헤드리스 브라우저로 검증: 사용자 예시값을 상승률 0%·연간 임대료 2억원×15년(=30억원)+공동접속료 10억원(일시금)+사전개발비 60억원(1회)+운영수익 0으로 입력하면 "생애주기 누적 총 수취액"이 정확히 100.00억원으로 계산됨을 확인. "반영" 클릭 시 상단 산정 기준이 "당사 이익 목표"로 전환되고 목표 당사 이익이 100.0으로 채워져 적정 입찰단가가 즉시 재계산됨을 확인(기존 기본값도 100이라 이번 케이스는 값이 그대로였으나 전환·재계산 자체는 정상 동작). **레거시 "목표 IRR 역산" 모드로 전환해 골든마스터 재검증**: 기본 입력값(임대료 1.5/사전개발비 7.5/공동접속료 2.5/운영수익 4)에서 가격배율 0.2783배·달성 IRR 9.00%·NPV 1.57억원·총수익 38.31억원 — 이전 라운드의 원본 골든마스터와 완전히 동일한 값이 재현되어 리팩터가 기존 로직을 훼손하지 않았음을 확인. 콘솔 오류 0건.
- 자체 발견 반영: 산출 총액 섹션 라벨이 "가격배율 반영 후 실제 적용 금액"으로 고정돼 있어 총액 직접 입력 모드(가격배율 없음)에서 오해 소지가 있음을 발견해 "총액 직접입력 모드에서는 입력값과 동일, 목표 IRR 역산 모드에서는 가격배율 반영"으로 라벨을 모드 중립적으로 수정.

**시나리오별 전체 모델 `.json` 다운로드 + 입찰단가 최저·최고 범위 표시 (2026-09-03)**: 위 CSV 3절 확장과 같은 갈래에서 함께 반영된 두 기능 — 아티팩트가 세션 도중 여러 차례 재게시되어(다른 세션에서 이어서 작업) 뒤늦게 발견, 전체 소스를 다시 읽고 CDP 헤드리스 테스트(`test-prototype-v13.mjs`)로 직접 검증함:
- **시나리오별 `.json` 다운로드**: "현재값 저장" 클릭 시 localStorage 저장과 동시에 그 시나리오의 입력모델 전체(`inputs`=복원용 필드값, `kpi`=저장 시점 요약지표, `assumptionDisplay`=Assumption 절과 동일한 라벨 붙은 전체 입력값, `yearlyDetail`=Appendix와 동일한 연도별 상세)를 `ESS_시나리오_<이름>_<날짜>.json` 파일로 즉시 다운로드(`downloadScenarioModel()`). localStorage가 지워지거나 다른 사람과 공유해야 할 때 이 파일 하나로 그 시점 값을 완전히 복구할 수 있도록 하기 위함이며, 저장된 시나리오 목록의 "다운로드" 버튼으로 언제든 재다운로드 가능.
- **입찰단가 범위 표시**: 결과 카드에 "최저단가"(P-IRR 6%·당사 이익 100억 15년 누적을 동시에 만족하는 최소 입찰단가, `computeFloorPrice()`가 두 조건을 각각 이분탐색해 더 빡빡한 쪽을 채택)와 "최고단가"(좌측 산정 기준으로 구한 현재 적정 입찰단가)를 막대 그래프로 병기, 현재 산정 결과가 최저 하한에 못 미치면 경고 배너 표시.
- 헤드리스 브라우저로 검증: 시나리오 2개 저장 시 `downloads.save()`가 각각 올바른 파일명·`inputs/kpi/assumptionDisplay/yearlyDetail` 4개 키·`yearlyDetail.ok=true`·연도별 상세 17행으로 정확히 호출됨을 확인. KCH 개발수수료 절 기본값(총액 직접 입력)의 "생애주기 누적 총 수취액" 137.67억원을 "▶ 상단에 반영" 클릭 시 목표 당사 이익이 137.7억원으로, 산정 기준이 "당사 이익 목표"로 전환되고 적정 입찰단가가 22.51→24.47원으로 정상 재계산됨을 확인. 최저·최고단가 막대가 각각 23.01원(당사이익 100억 기준)·24.97원으로 표시되고 하한 초과 시 경고가 나타나지 않음(정상 범위 내)을 확인. CSV Appendix의 "총투자비=조달합계 검증" 행이 정확히 0.000000으로 나와 조달내역·총투자비 간 정합성이 재무적으로 성립함을 재확인. 콘솔 오류·예외 0건.

**발전원별(ESS) 모델링 한계 상시노출 (2026-09-03)**: 이터레이션 3 남은 범위 항목 중 "발전원별 모델링 한계 안내가 결과 화면에 항상 보인다"를 진행 요청받음. 기존에도 페이지 맨 아래 "확정이 필요한 항목" 박스에 12개 항목이 상세히 정리돼 있었지만, 결과를 보려면 스크롤을 끝까지 내려야 해서 "결과 화면에 항상 보인다"는 DoD 문구를 충족하지 못한다고 판단(발전원 4종 확장 전이라 "발전원별"은 우선 ESS 단일 기준으로 적용, 확장 후 동일 패턴을 다른 발전원에도 복제하면 됨):
- 오른쪽 `.sticky`(스크롤해도 항상 화면에 고정) 결과 카드 맨 아래에 "⚠ ESS 모델링 한계 (이 결과에 항상 적용됨)" 박스를 신규 추가 — 기존 12개 항목 중 의사결정에 가장 직접적인 영향을 주는 5개를 골라 축약 노출(재무 프리셋이 참고자료 기반 초안값이라는 점·CAPEX/OPEX가 예시 규모라는 점·부채 사이징이 고정비율 방식만 지원한다는 점·준공지연 1단계 감액식과 이행률 패널티가 초안 해석/근사치라는 점·연간 단위 모델이라는 점), "▶ 전체 12개 항목 상세는 페이지 하단 '확정이 필요한 항목' 참고" 링크로 기존 상세 목록(`#modelCaveatsFull` 앵커 추가)에 바로 이동 가능하게 함. 기존 미리 쓰던 CSS `.note-box`(정의만 있고 실제로는 안 쓰이고 있었음)를 재사용.
- 이 박스는 계산 결과(`recalcAll()`)와 무관하게 정적 HTML로 항상 렌더링되도록 구현 — 목표 미달·준공지연 3단계(계약 해지) 등 산정이 실패한 상태에서도 "결과가 없으니 한계도 안 보인다"가 되지 않고 계속 노출됨.
- 헤드리스 브라우저로 검증: 기본값에서 박스가 정상 렌더링되고 하단 앵커 링크가 정확히 `#modelCaveatsFull`을 가리킴을 확인. 준공지연 3단계(계약 해지)로 전환해 `heroBidPrice`가 "N/A"가 된 상태에서도 모델링 한계 박스는 동일하게 표시됨을 확인(계산 성공 여부와 무관하게 상시노출 요구를 충족). 콘솔 오류·예외 0건.

**반응형·접근성 마무리 (2026-09-07)**: 이터레이션 3 마지막 범위 항목 "반응형·접근성 마무리(모바일 폭 대응, 스크린리더 라벨 전수 점검)" 진행. 두 갈래로 나눠 작업:
- **접근성 — label-for 전수 점검**: 전체 97개 `<label>` 중 `for=` 속성이 붙은 것이 0개(전무)임을 발견 — 스크린리더가 라벨과 입력 필드를 프로그램적으로 연결하지 못하는 상태였음. 정적 HTML 89개는 스크립트(정규식 치환)로 일괄 수정, CAPEX 13항목·OPEX 4항목·운영연차별 가동률(동적 생성 행, `buildCapexRows`/`buildOpexRows`/`syncOperatingRateRows`)은 `<span>`으로만 라벨을 표시하거나 `id` 자체가 없어 스크립트가 못 건드린 17개는 각 항목에 고유 `id`(`capexItem${i}`/`opexItem${i}`/`opRate${i}`)를 부여하고 `<label for=...>`로 직접 수정, 세무·배당 프리셋(읽기전용) 6개 필드도 `id` 신설 후 연결 — 최종 127개 라벨 전부 `for=` 연결 완료(회귀 없음, id 충돌 0건 확인). 부가로 검증용 `.warn` 13개 전부에 `role="status"`(동적으로 나타나는 검증 메시지를 스크린리더가 즉시 안내하도록) 추가, `<summary>` 안의 장식용 화살표(▶) 5개에 `aria-hidden="true"` 추가(네이티브 `<details>`가 이미 열림/닫힘 상태를 안내하므로 화살표 문자가 중복 낭독되지 않도록).
- **반응형 — 768/480/375px 실측**: 플랫폼이 씌우는 `<meta name="viewport">`가 원본 파일(`file://`) 단독 로드 시에는 빠져 있어 실제 배포 환경을 재현하지 못한다는 점을 발견 — 뷰포트 메타를 포함한 임시 래퍼로 감싸 테스트한 결과, 480px·375px에서 실제 페이지 가로 스크롤(오버플로)이 발생함을 확인. 근본 원인은 CSS Grid의 잘 알려진 함정(그리드/플렉스 아이템의 기본값 `min-width:auto`가 콘텐츠의 고유 너비 이하로 줄어드는 것을 막아, 부모 그리드 전체가 컨테이너보다 넓어짐) — 3곳에서 동시에 발생: ① 980px 이하 미디어쿼리가 데스크톱 2열(`.grid`)을 1열로 바꾸면서 안전장치였던 `minmax(0,1fr)`을 빼먹고 `1fr`만 남겨 재발, ② `.field`(입력 필드 그리드 아이템) 자체에 `min-width:0`이 없어 `.field-grid`의 `auto-fit` 컬럼 수 계산이 왜곡, ③ 시나리오 목록 행(`.item-row`, 이름·단가·IRR·불러오기·다운로드·삭제 6열 고정)이 좁은 화면에서 그대로 유지되며 버튼 3개가 짓눌림. 각각 ① 미디어쿼리를 `minmax(0,1fr)`로 복원 ② `.field { min-width:0; }` 신규 규칙 추가 ③ 640px 이하에서 `.item-row.scenario-row`를 1열로 재배치하는 미디어쿼리 추가로 해결. 부수적으로 시나리오 카드 헤더의 버튼 2개(CSV 다운로드/전체삭제)와 "운영연차별 가동률"·"CAPEX 입력 방식" 헤더의 버튼도 좁은 화면에서 글자 단위로 줄바꿈되며 짓눌리는 것을 발견해 `flex-wrap:wrap`+버튼 `white-space:nowrap`으로 함께 수정.
- 헤드리스 브라우저로 검증(뷰포트 메타 포함 래퍼로 실제 배포 환경 재현): 시나리오 2개 저장 후 768/480/375px 각각에서 모든 `<details>` 섹션을 펼친 상태로 `document.documentElement.scrollWidth === clientWidth`(가로 스크롤 0)를 확인, 8×7 Sensitivity 표처럼 본질적으로 넓은 표는 `.table-wrap`의 `overflow-x:auto`로 표 내부에서만 스크롤되고 페이지 전체 폭에는 영향 없음을 확인. 127개 라벨 전부 `label[for]`로 매칭됨(CAPEX 13/13·OPEX 4/4·가동률 15/15 포함)을 스크립트로 전수 확인, 라벨을 클릭하면 연결된 입력 필드로 포커스가 이동함(네이티브 브라우저 동작)을 확인. 반응형 수정 이후에도 계약용량 변경 시 재계산(26.48원/kWh로 정상 변경), 이상치 플래그(자기자본+주민채권 초과 경고), KCH 반영 버튼(목표 당사 이익 137.7억원 반영) 등 기존 기능이 전부 정상 동작함을 회귀 확인. 콘솔 오류·예외 0건.

**시나리오별 적정 입찰단가 막대그래프 추가 (2026-09-07)**: "만든 내용을 봤는데 단가 관련해서 그래프를 통해서 시각화해서 볼 수 있었으면 해"라는 요청. 단가 관련 시각화 후보 4가지(입찰가격-점수 곡선/시나리오별 막대그래프/Sensitivity 히트맵/연도별 매출-단가 추이) 중 "시나리오별 적정단가 막대그래프"를 선택받아 반영. `dataviz` 스킬 절차(폼 선택→색상→검증→마크 스펙→호버 레이어→접근성)를 따라 구현:
- **폼 선택**: 여러 시나리오 간 단일 지표(적정 입찰단가) 크기 비교이므로 막대그래프가 정답(스킬의 "Compare magnitude → bar/column, sequential 1-hue" 규칙과 일치). 시나리오 이름이 사용자 자유 입력이라 길어질 수 있어(예: "EPC 대폭 증가 시나리오") 세로 막대 대신 가로 막대로 구성해 이름이 잘리지 않게 함.
- **색상**: 단일 지표·단일 계열이라 accent 단색만 사용(범례 불필요 — 제목 "적정 입찰단가 비교"가 이미 무엇을 그리는지 말해줌). 페이지 기존 테마 토큰(`var(--accent)`)을 그대로 사용해 별도 팔레트 검증 없이 라이트/다크 테마에 자동 대응.
- **마크**: 18px 두께(스펙 상한 24px 이내), 데이터가 자라나는 끝(오른쪽)만 4px 라운드·기준선(왼쪽)은 사각(스펙 그대로), 값 라벨은 막대 내부가 아니라 전용 컬럼에 배치해 어떤 값 크기에서도 잘리지 않게 함(라벨 클리핑 금지 규칙 준수).
- **호버 레이어**: 막대 자체가 히트 타겟 — `pointermove`로 시나리오명·적정단가·P-IRR을 보여주는 툴팁을 표시, 마우스뿐 아니라 키보드 포커스(`focusin`/`focusout`)에서도 동일한 툴팁이 뜨도록 구현(스킬의 "탭에서도 같은 정보" 요구사항). 각 막대에 `role="img"`+`aria-label`도 부여해 스크린리더가 호버 없이도 값을 읽을 수 있게 함. 라벨/이름은 `textContent`로만 주입(사용자가 입력한 시나리오 이름은 신뢰할 수 없는 데이터이므로 `innerHTML` 금지 규칙 준수).
- 헤드리스 브라우저로 검증: 시나리오 3개(22.51/23.01/29.11원) 저장 시 막대 폭이 정확히 비례(77.3%/79.0%/100%)로 렌더링됨을 확인, 마우스 호버·키보드 포커스 양쪽에서 툴팁에 시나리오명·단가·P-IRR이 정확히 표시되고 포커스 해제 시 사라짐을 확인, 시나리오 삭제 시 그래프가 즉시 재렌더링(3개→2개)됨을 확인, 시나리오가 하나도 없을 때는 빈 상태로 정상 처리됨을 확인. 라이트/다크 테마 스크린샷으로 대비·가독성 육안 확인. 콘솔 오류·예외 0건.

**시나리오 카드 배치 조정 + 참고용 계산 필드 5개 숨김 (2026-09-07)**: "보기가 상당히 불편하네, 해당 내용이 맨 위로 가도록 조정하고 01~05절 내용 중 다음은 불필요하니 제외해줘"라는 요청(뒤에 제외 목록이 끊겨 재확인 후 "① 설치 저장용량·설치 출력용량·저장시간·개발기간, ② 설치용량"으로 확정). 두 가지 반영:
- **차트 위치 조정**: 방금 추가한 "적정 입찰단가 비교" 막대그래프가 시나리오 이름 입력란·경고·시나리오 목록보다 아래에 있어 한참 스크롤해야 보이던 것을 카드 맨 위(부제 바로 아래, 입력란보다 위)로 이동.
- **참고용 계산 필드 숨김**: 1절의 "설치 저장용량"(`totalStorageMWh`)·"설치 출력용량(PCS 정격 합계)"(`totalPowerMW`)·"저장시간(설치 기준)"(`durationHours`)·"개발기간"(`developmentYears`), 2절의 "설치용량(참고)"(`installedCapacityDisplay`) 5개 필드를 화면에서 숨김. 계산 로직·`id`는 그대로 두고 각 필드를 감싼 `.field` 래퍼에 `style="display:none"`만 추가하는 방식으로 처리 — `readModel()`이 여전히 이 요소들에 값을 쓰고(`$('...').value = ...`) 다른 곳(예: 계약용량 초과 경고)이 여전히 그 계산 결과(`installedCapacityMW` 등 내부 변수)를 참조하므로, 완전히 제거하면 `null` 참조 오류가 나거나 관련 경고 로직이 깨질 위험이 있어 "안 보이지만 계속 계산됨" 방식을 선택.
- 헤드리스 브라우저로 검증: 새로고침 후 카드 자식 순서가 `부제→막대그래프→이름입력란→...`으로 바뀌었음을 확인. 5개 필드 모두 `getComputedStyle(...).display === 'none'`이면서 동시에 `.value`에는 정상 계산값(732.5/99.8/7.34/1/99.8)이 그대로 들어있음을 확인(안 보일 뿐 계산은 안 끊김). 계약용량을 150MW로 올려 설치용량(99.8MW, 이제 화면엔 안 보임)을 초과시켜도 "계약용량이 설치용량을 초과합니다" 경고가 여전히 정확하게 뜨는 것으로 내부 계산 연쇄가 끊기지 않았음을 재확인. 콘솔 오류·예외 0건.

**당사 이익↔목표 IRR 환산값 실시간 표시 (2026-09-07)**: "당사이익과 목표 IRR이 연동되어 나타났으면 좋겠어"라는 요청. 반영 방식을 확인한 결과 "산정 기준(당사이익목표/목표IRR 중 하나만 고르는 구조)은 그대로 두고, 하나를 입력하면 다른 하나의 환산값을 즉시 표시"(추천안)를 선택받아 반영:
- "목표 당사 이익" 입력란 바로 아래에 `crossRefIrr` 캡션을, "목표 IRR" 입력란 바로 아래에 `crossRefProfit` 캡션을 신규 추가하고 `syncSolveModeVisibility()`에서 활성 산정 기준의 반대쪽만 보이도록 토글. 별도 재계산(이분탐색) 없이 이미 `solveCurrentModel()`이 계산해 둔 결과(`r.projectIrr`, `r.companyProfitPreTax`)를 그대로 읽어서 표시 — 산정 기준 자체(어느 쪽으로 입찰단가를 역산하는지)는 전혀 바뀌지 않고 참고용 정보만 추가되는 구조라 회귀 위험이 낮음.
- "당사 이익 목표" 모드에서는 "↔ 이 조건의 환산 P-IRR: X.XX% (참고 — 산정 기준은 그대로 "당사 이익 목표")"를, "목표 IRR" 모드에서는 "↔ 이 조건의 환산 당사 이익(누적 순이익 세전): XXX.X억원 (참고 — 산정 기준은 그대로 "목표 IRR")"을 표시. 목표 당사 이익 또는 목표 IRR 입력값을 바꿀 때마다 `recalcAll()`이 재실행되며 이 캡션도 함께 즉시 갱신됨.
- 헤드리스 브라우저로 검증: 기본값(당사이익목표 100억원)에서 환산 P-IRR(7.86%)이 우측 "달성 P-IRR" KPI와 정확히 일치함을 확인. 목표 당사 이익을 200억원으로 올리면 환산 P-IRR이 12.38%로, 달성 P-IRR도 동일하게 12.38%로 함께 바뀜을 확인. "목표 IRR" 모드로 전환하면 환산 당사이익(102.9억원)이 "당사 누적 순이익 세전/세후" KPI의 세전 값(102.9)과 정확히 일치함을 확인, 목표 IRR을 12%로 올리면 환산 당사이익이 191.3억원으로 갱신됨을 확인. 목표 IRR을 비현실적으로 높여(100,000%) 솔브가 실패하면 두 캡션 모두 빈 문자열로 정상 초기화됨을 확인(이전 값이 남아있지 않음). 콘솔 오류·예외 0건.

**서식 적용 다중 시트 .xlsx 생성기 — KCH 수수료·가격환산 시트 추가 + 원클릭 .bat (2026-09-10)**: "엑셀 회신하는 부분의 엑셀이 `ESS_적정입찰단가_모델_기본안.xlsx` 수준으로는 나와야 가독성이 있는데 안 될까?"라는 질문. 확인 결과 ① 아티팩트의 `downloads` 기능은 플랫폼이 허용하는 확장자가 고정돼 있어 `.xlsx`를 낼 수 없고(하드 제한), CSV는 정의상 시트·색상·서식이 없어 그 수준이 원천적으로 불가능하며, ② 그러나 말씀하신 벤치마크 파일 자체가 프로젝트 폴더의 로컬 생성기 `ess-bidprice-xlsx.mjs`(Node + ExcelJS, 아티팩트 계산 엔진을 그대로 복사)의 출력물이라는 것을 확인 — 생성기를 다시 실행해 벤치마크와 셀 단위로(7개 시트·행/열·틀고정·값·숫자서식·굵게·배경색) 동일함을, 복사된 엔진 함수 19개가 아티팩트 현재 코드와 전부 동일함을 검증. 이를 바탕으로 "둘 다"(① 원클릭화, ② KCH 시트 추가)를 선택받아 반영:
- **KCH 시트 신설**: 아티팩트 6.4절(`kchBuildCashflow`/`kchIrrAt`/`kchSolveScaleFactor`)을 그대로 복사하고 DOM 의존 `kchReadModel()`만 순수 함수(`kchReadModelFromFields`)로 옮김. 산정 방식·사업 기본정보·KCH 비용·수익원 4종 입력·결과(가격배율(역산 모드)·달성 IRR·NPV·총수익·총비용·생애주기 누적 총 수취액)·실제 적용 금액·연차별 현금흐름(①~④ 수익원 − 비용 = 순현금흐름, 운영 종료까지)·민감도표(가격배율 11단 × 설비용량 7단, 목표 IRR 이상 초록)를 담음.
- **가격환산 시트 신설**: 6.4→6.5 자동환산(`totalLifetimeGenerationKWh` 복사, 수취액×1e8÷총발전량)과 6.5 가격환산계산기 산식(`recalcPriceScore` 복사 — 가격평가점수·방법 A/B-1/B-2/B평균·Δ점 표)을 담음. 아티팩트가 6.5 입찰가격 입력란에 `(순수단가+환산분).toFixed(2)`를 써넣고 그 2자리 반올림값으로 점수를 계산하는 순서를 그대로 따라야 화면과 0.01점까지 일치한다는 것을 발견해(반올림 없이 계산하면 38.08 vs 화면 38.09) 동일 순서로 수정. 시나리오 JSON에 기록된 "저장 당시 화면의 6.5 입찰가격"을 함께 적고, 자동연동 값과 다르면(사용자가 직접 입력해 연동을 끊은 경우) 경고 행을 넣어 어느 값 기준인지 드러나게 함.
- **원클릭 `ESS_엑셀변환.bat`**: 더블클릭 → 기본값으로 `ESS_적정입찰단가_모델_최신.xlsx` 생성, 아티팩트에서 받은 시나리오 `.json`을 끌어다 놓기 → 같은 폴더에 같은 이름의 `.xlsx` 생성, 완료 후 Excel로 자동 열기. 첫 버전은 `echo` 문구 안의 괄호 `(96MW 기본안)`이 cmd.exe의 `( … )` 블록 종결자로 해석돼 "사용) was unexpected"로 죽는 문제가 있어 괄호를 전부 제거하고 블록 구조를 단순화. 한글 경로·메시지를 위해 `chcp 65001` + UTF-8(BOM 없음)로 저장, Node가 PATH에 없을 때 `C:\Program Files\nodejs`로 자동 보완, `ESS_NO_OPEN=1`이면 Excel 실행 생략(자동 테스트용). 생성기와 같은 정책으로 `.gitignore`에 추가(앱과 무관한 로컬 도구).
- **검증**: (a) 기본값 직접입력 모드 — 수취액 137.67억원·환산분 +1.13원/kWh·6.5 입찰가격 23.63원·점수 38.09점으로 아티팩트 화면과 일치. (b) 레거시 목표 IRR 역산 모드(JSON으로 `kchCalcMode: "irrSolve"` 지정) — 가격배율 0.2783배·달성 IRR 9.00%·NPV 1.57억원·총수익 38.31억원·총비용 23.50억원·민감도표 1.0배 행 17.0/28.7/39.8/50.8/61.7/72.7/83.8% = 기존 골든마스터 그대로. (c) **실제 아티팩트가 내보낸 JSON으로 종단간 검증**: 헤드리스 브라우저에서 `downloads`를 스텁한 채 계약용량 80MW·선순위금리 6%·목표이익 120억·KCH 임대료 3억/사전개발비 10억·최저입찰가 19원으로 바꿔 시나리오를 저장해 실제 다운로드되는 `.json`을 확보한 뒤 생성기에 투입 → Summary(적정단가 28.10·P-IRR 9.13%·NPV 335.5·총투자비 1,133.1·당사순이익 120.0)·KCH(수취액 166.11·IRR 64.09%·NPV 73.41·총비용 23.50)·가격환산(원값 28.10·환산분 1.63·6.5 입찰가격 29.73·저장 당시 화면값 29.73·점수 31.95·B평균 0.9316) **15개 셀 전부 화면값과 일치**, 불일치 경고 행 없음. (d) `.bat`를 탐색기 실행과 같은 방식(직접 실행)으로 인자 없이/JSON 인자로 각각 구동해 두 경로 모두 정상 종료·파일 생성 확인. 콘솔/페이지 예외 0건.
- 한계: 이 xlsx는 여전히 아티팩트 "안"에서 받는 게 아니라 로컬에서 만드는 것이며(플랫폼 제한), 생성기는 아티팩트 엔진의 복사본이라 아티팩트 계산 로직을 바꿀 때마다 같이 갱신해야 함(엔진 함수 diff 검사를 위 검증에 포함해 둠).

---

## 10. 리스크 및 열린 질문

| 항목 | 내용 | 확인 필요 |
| --- | --- | --- |
| Goal Seek → 이분탐색 이식 | Excel의 Goal Seek을 웹에서는 수치해석(이분탐색)으로 대체한다. 극단적 입력값에서 수렴 실패 가능성 | 수렴 실패 시 사용자에게 명확한 오류 메시지 필요 |
| 베타모델(ESS 베타테스트용.xlsm) 연동 | 현재 실제 프로토타입은 그 파일을 직접 열지 않는다. 값은 프로토타입 자체 계산으로 대체 | 실제 안좌 BESS 수치가 채워지면 프리셋 기본값 업데이트 필요 |
| 세율·금융 프리셋 갱신 | 역사적 예시 프리셋을 언제 현행 기준으로 교체할지 | 재무팀 확인 필요 |
| 발전원별 차별 필드 완성도 | 이터레이션 1은 ESS만 검증 — PV/ON/OF는 이터레이션 2에서 처음 실동작 확인 | 이터레이션 2 착수 전 필드 목록 재확인 |

---

## 11. 부록 — 참고 자료

- `변수정의서_초안_이터레이션1.md` (5개 섹션 변수 초안 + 가격환산계산기·단가산정모델 연계 구조)
- `과제정의서_3팀_서신준.md` (기획문서 본체, 8개 장 전체)
- 서비스 프로세스 다이어그램 (Artifact: 재무모델 사업성 분석 흐름도)
- `ESS_단가산정모델.xlsx` (KCH 개발수수료 IRR 역산 로직 원본)
- `ESS_비가격점수_가격환산_계산기_1.xlsx`, ESS 가격환산 계산기 페이지 (가격평가점수 산식 원본)
- 산식 출처: 한국전력거래소 공고 제2025-05호 「2025년 제2차 ESS 중앙계약시장(육지) 경쟁입찰 공고」 Ⅲ.4·Ⅲ.5
- 기존 코드베이스: `js/project-data.js`, `js/finance.js`, `js/form-view.js`, `js/results-view.js`, `js/app.js`
